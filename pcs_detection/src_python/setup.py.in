from distutils.core import setup

if __name__ == '__main__':
    setup(name='pcs_detection',
          version='${PACKAGE_VERSION}',
          package_dir={ '': '${CMAKE_CURRENT_SOURCE_DIR}' },
          packages=['pcs_detection', 'pcs_detection.models'])
